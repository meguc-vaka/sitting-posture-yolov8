# 坐姿記錄模式大改 — 變更彙整 (2026-08)

## 一、核心架構：從「定時寫入」改為「Session 彙總制」

| 舊制 | 新制 |
|---|---|
| 每 3 分鐘寫一筆到 `posture_records` | 每次監測結束寫一筆彙總到 `monitoring_sessions` |
| 一天約 480 筆，無 user_id | 一天約 10-20 筆，綁定 user_id |
| 所有用戶數據混在一起 | 個人數據獨立，管理員可看總覽 |

## 二、15 秒穩定判定機制

- 同一姿勢需連續維持 150 幀（15 秒 × 10fps）才開始計入統計
- 姿勢變更時立即歸零重算
- 過渡期（姿勢切換後 15 秒內）不計入任何姿勢
- 避免短暫姿勢波動汙染數據

## 三、2 分鐘定時提示系統

- 每 2 分鐘檢查一次 `bad_posture_sustained` 旗標
- 旗標為 True（過去 2 分鐘內曾有不良姿勢穩定超過 15 秒）→ 發送 `ai_alert` 提示
- 提示後冷卻 1 分鐘，避免連續轟炸
- 開始監測後首次檢查立即觸發，不需等 2 分鐘

## 四、新增資料表 `monitoring_sessions`

| 欄位 | 說明 |
|---|---|
| `session_id` | UUID，唯一識別 |
| `user_id` | 綁定使用者 |
| `start_time` / `end_time` | 監測起訖時間 |
| `good_frames` / `turtle_frames` / `down_frames` / `slouch_frames` | 四種姿勢穩定幀數 |
| `dominant_posture` | 主要姿勢（佔比最高） |
| `posture_ratio` | JSON 格式姿勢佔比 `{"good":60,"turtle":20,...}` |
| `avg_angle` | 平均脊椎內角（度） |
| `avg_offset` | 平均耳肩偏移（px） |
| `image_path` | 最差姿勢截圖 |

## 五、路由改動

| 路由 | 改動 |
|---|---|
| `/renaissance` | 改撈 `monitoring_sessions`，管理員看全部用戶+姓名，一般用戶看自己 |
| `/analysis` | 改撈 `monitoring_sessions`，管理員看全部彙整，一般用戶看自己 |
| `/rank` | 從假資料改為真實 `monitoring_sessions` 彙整 |
| `/aboutus` | 新增路由，渲染團隊介紹頁 |
| `POST /login` | 新增 `session['email']` 寫入 |

## 六、WebSocket 事件改動

| 事件 | 改動 |
|---|---|
| `video_frame` | 新增 15 秒穩定判定、角度累積、2 分鐘提示檢查 |
| `start_monitoring` | 新增事件，重置所有狀態、記錄 session 開始時間 |
| `stop_monitoring` | 新增事件，觸發 `write_session_summary()` 彙總寫入 |
| `connect` / `disconnect` | 改用 `write_session_summary()` 結算 |

## 七、前端改動

| 檔案 | 改動 |
|---|---|
| `index.html` | 偵測時間改為 10/25/45/60/無限制 分鐘 |
| `index.js` | 新增 `start_monitoring` / `stop_monitoring` emit；修復 modal backdrop 殘留導致畫面卡死 |
| `record.html` | 刪除「欄位設定」按鈕；脊椎內角顯示度數 |
| `login.html` | 新增 `col-12` fallback |
| `aboutus.html` | 新建團隊介紹頁（深色毛玻璃風格） |
| `changePassword.html` | 重寫為深色風格 |
| `editProfile.html` | 重寫為深色風格 |
| `profileHome.html` | 重寫（頭像+資料卡+紀錄區） |

## 八、全域手機適配

全部 10 個頁面統一加上：
- `viewport` meta tag
- Header `col-6 col-md-4/8` 響應式柵格
- `navbar-expand-sm navbar-dark` + 漢堡選單
- 手機隱藏搜尋框/關於我們按鈕
- 各頁面專屬 `@media (max-width: 767.98px)` 規則

## 九、刪除的舊函式

- `insert_posture_record_if_any()` — 已被 `write_session_summary()` 取代
- 舊的 AI 警告滑動窗口（`posture_history` 30 幀判定）— 已被 2 分鐘定時提示取代
- `rank` 路由中的假資料（mock data）— 已改為真實查詢