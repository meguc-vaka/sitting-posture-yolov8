# 智能坐姿監測與管理系統 — 後端模組說明文件 (app.py & db.py)

> **文件類型**：系統架構與 API 模組對照  
> **目標對象**：系統管理者 / 後端開發人員  
> **涵蓋模組**：`app.py`, `db.py`

---

## 📋 架構導覽說明
本文件將程式碼結構整理為六大核心模組：**核心配置與初始化**、**使用者身份驗證與個人資料管理**、**即時坐姿識別與 WebSocket 通訊**、**數據分析與視覺化路由**、**自動化排程與每週郵件通知**，以及**系統進入點與資料庫模組**。

---

## 一、 核心配置與初始化 (Initialization & Configuration)

說明系統啟動時載入之套件、全域變數與模型資源。

| 模組 / 功能區塊 | 程式碼行數 | 職責說明 |
| :--- | :--- | :--- |
| **Python 標準庫模組** | 第 1 - 5 行 | 載入時間、系統等基礎庫。 |
| **第三方套件模組** | 第 7 - 11 行 | 載入 Flask, SocketIO, APScheduler 等外部套件。 |
| **專案內部模組** | 第 13 - 15 行 | 載入自訂資料庫操作模組 (`db.py`)。 |
| **Flask 應用與核心元件** | 第 17 - 19 行 | 初始化 Flask App 並與 SocketIO 伺服器綁定。 |
| **全域變數與狀態控制** | 第 21 - 28 行 | 設定系統短期記憶、警告冷卻時間與全域狀態。 |
| **AI 模型初始化** | 配置區塊 | 載入坐姿識別與骨架標記 AI 模型與預設參數。 |
| **資料庫初始化** | 配置區塊 | 建立資料庫連線池與連線開關配置。 |

---

## 二、 使用者身份驗證與個人資料管理 (User Auth & Profile)

處理使用者登入、登出以及個人資料與密碼修改。

### 1. 帳號登入系統
* **路由**：`@app.route('/login', methods=['POST'])`
* **函式**：`def login()`
* **說明**：處理使用者登入驗證，確認憑證並建立 Session 狀態。

### 2. 帳號登出系統
* **路由**：`@app.route('/logout')`
* **函式**：`def logout()`
* **說明**：清除當前使用者的 Session 登入狀態並重定向至首頁。

### 3. 獲取當前登入者資料 (Helper)
* **函式**：`def getLoginDetails()`
* **說明**：驗證當前 Session 是否有效，若已登入則從資料庫查詢並傳回該使用者的 ID 與姓名。

### 4. 呈現個人主頁
* **路由**：`@app.route("/profileHome")`
* **函式**：`def profileHome()`
* **說明**：從資料庫撈取個人詳細資料，並渲染 `profileHome.html` 頁面。
* **內部呼叫**：`getLoginDetails()`

### 5. 載入編輯個人資料頁面
* **路由**：`@app.route("/editProfile")`
* **函式**：`def editProfile()`
* **說明**：載入個人資料編輯表單頁面。
* **內部呼叫**：`getLoginDetails()`

### 6. 個人資料更新處理
* **路由**：`@app.route("/updateProfile", methods=["GET", "POST"])`
* **函式**：`def updateProfile()`
* **說明**：接收表單提交資料（姓名、地址、電話、體重、身高）並寫回資料庫，完成後重定向。

### 7. 修改密碼功能
* **路由**：`@app.route("/account/profile/changePassword", methods=["GET", "POST"])`
* **函式**：`def changePassword()`
* **說明**：處理變更密碼功能。GET 請求時顯示修改密碼頁面；POST 請求時驗證舊密碼是否正確（使用 MD5），若正確則將新密碼更新至資料庫。

---

## 三、 即時坐姿識別與 WebSocket 通訊 (Real-time AI & WebSockets)

處理雙向即時影像串流、AI 骨架分析與坐姿紀錄寫入。

### 1. 寫入並重置坐姿紀錄 (Helper)
* **函式**：`def insert_posture_record_if_any()`
* **說明**：檢查暫存區是否有坐姿數據，若有則將其寫入資料庫並重置即時監測計數器。

### 2. 處理 WebSocket 新連線
* **事件**：`@socketio.on('connect')`
* **函式**：`def handle_connect()`
* **說明**：WebSocket 連線建立時的「初始化與重置處理（Session Clearance/Reset）」。先存檔上一階段的坐姿數據，再重置即時監測與警告狀態。
* **內部呼叫**：`insert_posture_record_if_any()`

### 3. 接收影像幀進行 AI 姿勢識別與即時警告
* **事件**：`@socketio.on('video_frame')` (WebSocket 接收通道)
* **函式**：`def handle_frame(data)`
* **說明**：接收前端影像幀進行 AI 姿勢識別、骨架標記繪製、定期統計落庫與不良姿勢即時警告。
* **內部呼叫**：`insert_posture_record_if_any()`

### 4. 處理 WebSocket 斷線
* **事件**：`@socketio.on('disconnect')`
* **函式**：`def handle_disconnect()`
* **說明**：符合時間條件時強制將剩餘坐姿數據寫入資料庫。
* **內部呼叫**：`insert_posture_record_if_any()`

---

## 四、 數據分析與視覺化頁面路由 (Analytics & Dashboard)

提供歷史紀錄查詢、統計圖表與排行榜功能。

### 1. 取得坐姿歷史紀錄（含分頁與狀態判斷）
* **路由**：`@app.route('/renaissance')`
* **函式**：`def posture_record()`
* **說明**：從資料庫讀取使用者的坐姿歷史紀錄，並處理成帶有分頁（Pagination）與狀態標籤（Status Badges）的資料，最後渲染給前端網頁 (`record.html`) 呈現。

### 2. 取得最近 30 筆紀錄並渲染分析頁面
* **路由**：`@app.route('/analysis')`
* **函式**：`def posture_analysis()`
* **處理流程**：
  1. **資料庫撈取與反轉（ORDER & REVERSE）**：撈取最近 30 筆坐姿紀錄。
  2. **圖表數據拆分與統計（Data Processing）**：整理成圖表（折線圖與圓餅圖）所需的數據格式。
  3. **打包與傳遞（Render Template）**：將折線圖與圓餅圖所需的數據打包成 `chart_data` 字典，並傳遞給 `analysis.html` 前端網頁。

### 3. 不良坐姿統計排行榜與改善建議
* **路由**：`@app.route('/rank')`
* **函式**：`def posture_rank()`
* **處理流程**：
  1. **資料準備（Mock Data）**：載入不良坐姿的排行榜資料（含改善建議與比例）。
  2. **頁面渲染（Render Template）**：渲染至排行榜前端頁面。

---

## 五、 自動化排程服務與每週郵件通知 (Scheduler & Email Service)

配置背景定時任務，定期寄送通知信件。

### 1. 配置排程器與郵件服務
* **行數**：第 359 - 372 行
* **說明**：設定 APScheduler 排程器並配置 Gmail SMTP 郵件寄送服務參數。

### 2. 取得一般使用者清單 (Helper)
* **函式**：`def get_all_users_from_db()`
* **說明**：從資料庫中搜尋（撈取）所有「非管理者（一般用戶）」的姓名與電子郵件資訊。

### 3. 批次發送用戶每週通知信 (Task)
* **函式**：`def send_weekly_email_to_all_users()`
* **說明**：觸發每週郵件發送流程，遍歷所有一般用戶並寄送週報通知。
* **內部呼叫**：`get_all_users_from_db()`

### 4. 排程器每週觸發點 (Trigger)
* **說明**：排程器每週定期自動觸發。
* **內部呼叫**：`send_weekly_email_to_all_users()`

---

## 六、 系統進入點與資料庫模組 (Entry Point & db.py)

### 1. app.py 主程式入口
* **行數**：第 536 - 541 行
* **說明**：主函式，啟動 Flask-SocketIO 伺服器與背景任務。

### 2. db.py 資料庫底層操作模組說明

| 核心功能區塊 | 程式碼行數 | 功能與職責說明 |
| :--- | :--- | :--- |
| **資料庫初始化** | 第 6 - 19 行 | 建立 SQLite/MySQL 資料庫連線，初始化資料表結構。 |
| **資料庫查詢 / 讀取** | 第 22 - 27 行 | 封裝 `SELECT` 查詢邏輯，回傳資料結果。 |
| **資料庫執行 / 寫入修改** | 第 30 - 34 行 | 封裝 `INSERT / UPDATE / DELETE` 操作，處理 Transaction 提交與 Rollback。 |
